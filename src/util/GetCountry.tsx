const GetCountry = () => {
    // if (!process.env.MULTISTORE || !(process as any).browser)
    return null;

    // return window?.location?.pathname?.split?.('/')?.[1]?.toLowerCase() || null;
};

export default GetCountry;
