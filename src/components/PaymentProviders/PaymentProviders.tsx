import React, { FunctionComponent, memo } from 'react';

interface PaymentProvidersProps {}
const PaymentProviders: FunctionComponent<PaymentProvidersProps> = (props) => {
    return <div className="PaymentProviders"></div>;
};

export default memo(PaymentProviders);
