/**
 * `@nordcom/commerce-test-viewport` — one source of truth for responsive
 * testing across every app and package: device/foldable viewport presets, a
 * `matchMedia` shim for unit tests, and a Playwright project factory for e2e.
 */

export * from './matchmedia';
export * from './playwright';
export * from './presets';
