---
"lspmesh": patch
---

Initial release: an LSP aggregator + MCP server that fronts TypeScript, Tailwind, and Biome behind one endpoint — routing each request to every backend that handles the file and merging the results — plus by-name `find_symbol` / `find_references` / `find_implementations` MCP tools.
