---
'@nordcom/cart-core': minor
'@nordcom/cart-react': minor
---

Add a `clear` cart mutation and `clear()` action so emptying the cart issues one bulk line removal instead of one `removeLine` per line.
