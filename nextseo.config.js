/* eslint-disable import/no-anonymous-default-export */
export default {
    // FIXME: Name should be per-store not hardcoded
    titleTemplate: `%s | ${process.env.DOMAIN === 'candybysweden.com' ? 'Candy by Sweden' : 'happysnus'}`
};
