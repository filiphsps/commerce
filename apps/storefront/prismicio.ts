import { accessToken, createClient, linkResolver, repositoryName } from '@/utils/prismic';

// Re-export the Prismic configuration to make slice-machine happy.
export { accessToken, createClient, linkResolver, repositoryName };
