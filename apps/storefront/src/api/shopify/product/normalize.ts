import 'server-only';

// No standalone type-adapter functions exist in this module.
// Shape normalization is performed inline within the API functions in `api.ts`
// and `pagination.ts`. This file is reserved for future pure adapters that
// convert raw Shopify shapes into the app's Product type.
