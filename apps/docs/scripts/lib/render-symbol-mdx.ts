// Placeholder — implemented in Task D4.
export type SymbolRenderArgs = Record<string, never>;

/**
 * Placeholder — returns an empty string until Task D4.
 *
 * @returns Empty string.
 */
export function renderSymbolMdx(_args: SymbolRenderArgs): string {
    return '';
}
