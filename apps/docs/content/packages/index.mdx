---
title: Packages
description: Per-workspace narrative docs for every package and app in the monorepo.
---

The Packages tab hosts authored narrative docs for every workspace. Each workspace
contributes a short overview plus a changelog mirror; the generated API reference
for the same workspace lives in the **Reference** tab.

Pick a category in the sidebar — applications, core, shopify, tagtree, or ui.
