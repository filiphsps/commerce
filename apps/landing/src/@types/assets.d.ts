// Re-declare Next's image-asset module types here so `import logo from '@/static/foo.svg'`
// type-checks even when `next-env.d.ts` isn't present (it's gitignored — only `next dev`
// and `next build` regenerate it).
/// <reference types="next/image-types/global" />
